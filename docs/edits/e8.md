path: /diffs/20250916\_153500\_ui\_pass.patch

```diff
diff --git a/src/App.jsx b/src/App.jsx
index 4a1b3b1..b9d8f33 100644
--- a/src/App.jsx
+++ b/src/App.jsx
@@ -1,34 +1,119 @@
-import React, { useRef, useState, useEffect } from 'react';
-import { useGameTimer } from './hooks/useGameTimer';
-import { useResponsiveLayout } from './hooks/useResponsiveLayout';
-import HudInfo from './components/HudInfo';
+import React, { useRef, useState, useEffect } from 'react';
+import { useGameTimer } from './hooks/useGameTimer';
+import { useResponsiveLayout } from './hooks/useResponsiveLayout';
+import HUD from './components/ui/HUD';
+import StartScreen from './components/ui/StartScreen';
+import GameOverOverlay from './components/ui/GameOverOverlay';
+import './styles/theme.css';
+import './styles/ui.css';
 
 export default function App() {
-  const canvasRef = useRef(null);
-  const minimapCanvasRef = useRef(null);
-  const playAreaRef = useRef(null);
-  const canvasWidthRef = useRef(0);
-  const canvasHeightRef = useRef(0);
-  const { start, pause, reset, formattedTime } = useGameTimer();
-  const { layout } = useResponsiveLayout({ canvasRef, minimapCanvasRef, playAreaRef, canvasWidthRef, canvasHeightRef });
-  const [state, setState] = useState('menu'); // 'menu' | 'playing' | 'gameover'
-  const [stats, setStats] = useState({ xp: 0, xpMax: 300, level: 1, lives: 3, wave: 1 });
+  const canvasRef = useRef(null);
+  const minimapCanvasRef = useRef(null);
+  const playAreaRef = useRef(null);
+  const canvasWidthRef = useRef(0);
+  const canvasHeightRef = useRef(0);
+  const { start, pause, reset, formattedTime } = useGameTimer();
+  const { layout } = useResponsiveLayout({ canvasRef, minimapCanvasRef, playAreaRef, canvasWidthRef, canvasHeightRef });
+
+  // 'menu' | 'playing' | 'gameover'
+  const [state, setState] = useState('menu');
+  const [stats, setStats] = useState({ xp: 0, xpMax: 300, level: 1, lives: 3, wave: 1, score: 0 });
+  const [lastRun, setLastRun] = useState({ level: 1, wave: 1, time: '00:00:00', score: 0 });
+
+  useEffect(() => {
+    if (state === 'playing') start();
+    else pause();
+  }, [state]);
 
   // TODO hook up your actual game loop + collisions to update stats
-  useEffect(() => {}, []);
+  useEffect(() => {}, []);
 
   function handleStart(mode = 'waves') {
-    reset(); setState('playing');
+    reset();
+    // reset gameplay state here
+    setStats((s) => ({ ...s, xp: 0, level: 1, lives: 3, wave: 1, score: 0 }));
+    setState('playing');
   }
 
   function handleGameOver() {
-    setState('gameover');
+    setLastRun({ level: stats.level, wave: stats.wave, time: formattedTime, score: stats.score });
+    setState('gameover');
   }
 
   return (
-    <div ref={playAreaRef} style={{ position: 'relative' }}>
-      <canvas ref={canvasRef} width={layout.canvas.w} height={layout.canvas.h} />
-      <HudInfo stats={stats} minimapRef={minimapCanvasRef} />
-    </div>
+    <div ref={playAreaRef} className="outerSpace">
+      <div className="playfieldFrame" style={{ width: layout.canvas.w, height: layout.canvas.h }}>
+        <canvas ref={canvasRef} width={layout.canvas.w} height={layout.canvas.h} />
+      </div>
+
+      <HUD
+        xp={stats.xp}
+        xpMax={stats.xpMax}
+        level={stats.level}
+        lives={stats.lives}
+        wave={stats.wave}
+        time={formattedTime}
+        score={stats.score}
+        minimapRef={minimapCanvasRef}
+      />
+
+      {state === 'menu' && (
+        <StartScreen
+          onStartWaves={() => handleStart('waves')}
+          onStartSurvival={() => handleStart('survival')}
+        />
+      )}
+
+      {state === 'gameover' && (
+        <GameOverOverlay
+          level={lastRun.level}
+          wave={lastRun.wave}
+          time={lastRun.time}
+          score={lastRun.score}
+          onMainMenu={() => setState('menu')}
+          onPlayAgain={() => handleStart('waves')}
+        />
+      )}
+    </div>
   );
 }
diff --git a/src/components/ui/HUD.jsx b/src/components/ui/HUD.jsx
new file mode 100644
index 0000000..b2ac0a7
--- /dev/null
+++ b/src/components/ui/HUD.jsx
@@ -0,0 +1,120 @@
+import React from 'react';
+
+export default function HUD({ xp, xpMax, level, lives, wave, time, score, minimapRef }) {
+  const pct = Math.max(0, Math.min(1, xpMax ? xp / xpMax : 0));
+
+  return (
+    <div className="hudRoot">
+      <div className="xpBar">
+        <div className="xpFill" style={{ width: `${pct * 100}%` }} />
+        <div className="xpText">{`${xp} / ${xpMax} XP`}</div>
+      </div>
+
+      <div className="hudPanels">
+        <div className="hudPanel">
+          <div className="hudColumn">
+            <div className="hudLabel">Level</div>
+            <div className="hudValue">{level}</div>
+          </div>
+          <div className="hudColumn">
+            <div className="hudLabel">Lives</div>
+            <div className="hudValue">{lives}</div>
+          </div>
+          <div className="hudColumn">
+            <div className="hudLabel">Score</div>
+            <div className="hudValue">{score}</div>
+          </div>
+        </div>
+
+        <div className="hudPanel hudMinimap">
+          <canvas ref={minimapRef} width={140} height={80} />
+        </div>
+
+        <div className="hudPanel">
+          <div className="hudColumn">
+            <div className="hudLabel">Wave</div>
+            <div className="hudValue">{wave}</div>
+          </div>
+          <div className="hudColumn">
+            <div className="hudLabel">Time</div>
+            <div className="hudValue mono">{time}</div>
+          </div>
+        </div>
+      </div>
+    </div>
+  );
+}
diff --git a/src/components/ui/StartScreen.jsx b/src/components/ui/StartScreen.jsx
new file mode 100644
index 0000000..ce5f7d2
--- /dev/null
+++ b/src/components/ui/StartScreen.jsx
@@ -0,0 +1,54 @@
+import React from 'react';
+
+export default function StartScreen({ onStartWaves, onStartSurvival }) {
+  return (
+    <div className="overlayRoot">
+      <div className="panel modal">
+        <h1 className="title">ASTEROIDS</h1>
+        <p className="subtitle">Choose a mode</p>
+        <div className="row">
+          <button className="btn primary" onClick={onStartWaves}>Waves</button>
+          <button className="btn" onClick={onStartSurvival}>Survival</button>
+        </div>
+        <div className="help">
+          <p className="mono">Controls: ← → rotate, ↑ thrust, Space fire</p>
+        </div>
+      </div>
+    </div>
+  );
+}
diff --git a/src/components/ui/GameOverOverlay.jsx b/src/components/ui/GameOverOverlay.jsx
new file mode 100644
index 0000000..a2b87d1
--- /dev/null
+++ b/src/components/ui/GameOverOverlay.jsx
@@ -0,0 +1,45 @@
+import React from 'react';
+
+export default function GameOverOverlay({ level, wave, time, score, onMainMenu, onPlayAgain }) {
+  return (
+    <div className="overlayRoot">
+      <div className="panel modal">
+        <h2 className="title">GAME OVER</h2>
+        <div className="statsRow">
+          <div><span className="dim">Level</span> <strong>{level}</strong></div>
+          <div><span className="dim">Wave</span> <strong>{wave}</strong></div>
+          <div><span className="dim">Time</span> <strong className="mono">{time}</strong></div>
+          <div><span className="dim">Score</span> <strong>{score}</strong></div>
+        </div>
+        <div className="row">
+          <button className="btn" onClick={onMainMenu}>Main Menu</button>
+          <button className="btn primary" onClick={onPlayAgain}>Play Again</button>
+        </div>
+      </div>
+    </div>
+  );
+}
diff --git a/src/styles/theme.css b/src/styles/theme.css
new file mode 100644
index 0000000..9e1b2f8
--- /dev/null
+++ b/src/styles/theme.css
@@ -0,0 +1,46 @@
+:root {
+  --fg: #e8e8e8;
+  --fg-dim: #a9a9a9;
+  --bg: #050607;
+  --line: #bfbfbf;
+  --accent: #9cf06d;
+  --accent-2: #e3db6a;
+  --panel-bg: rgba(255,255,255,0.03);
+  --panel-blur: saturate(100%) blur(2px);
+  --radius: 10px;
+  --hud-height: 110px;
+}
+
+.mono { font-family: ui-monospace, SFMono-Regular, Menlo, Consolas, monospace; }
+.dim { color: var(--fg-dim); }
+
+/* Outer space and frame */
+.outerSpace {
+  position: relative;
+  min-height: 100vh;
+  background: radial-gradient(1200px 600px at 50% 10%, rgba(255,255,255,0.05), transparent 70%),
+              var(--bg);
+  color: var(--fg);
+}
+
+.playfieldFrame {
+  position: relative;
+  margin: 40px auto 0 auto;
+  border: 2px solid var(--line);
+  box-shadow: 0 0 0 1px rgba(255,255,255,0.04) inset;
+}
+
+/* Subtle vignette outside playfield */
+.outerSpace::after {
+  content: "";
+  position: fixed;
+  inset: 0;
+  pointer-events: none;
+  background: radial-gradient(70% 60% at 50% 45%, transparent, rgba(0,0,0,0.5) 80%);
+}
diff --git a/src/styles/ui.css b/src/styles/ui.css
new file mode 100644
index 0000000..a4b1b6a
--- /dev/null
+++ b/src/styles/ui.css
@@ -0,0 +1,133 @@
+/* HUD */
+.hudRoot {
+  position: fixed;
+  left: 0; right: 0; bottom: 0;
+  display: grid;
+  gap: 8px;
+  padding: 10px 14px 14px;
+}
+
+.xpBar {
+  position: relative;
+  height: 18px;
+  border: 2px solid var(--line);
+  border-radius: var(--radius);
+  overflow: hidden;
+  background: rgba(255,255,255,0.05);
+}
+.xpFill {
+  height: 100%;
+  background: linear-gradient(90deg, var(--accent), var(--accent-2));
+  box-shadow: 0 0 8px rgba(156, 240, 109, 0.35);
+}
+.xpText {
+  position: absolute; inset: 0;
+  display: grid; place-items: center;
+  font-size: 12px; color: var(--bg);
+  mix-blend-mode: screen;
+}
+
+.hudPanels {
+  display: grid;
+  grid-template-columns: 1fr 180px 1fr;
+  gap: 10px;
+}
+.hudPanel {
+  border: 2px solid var(--line);
+  border-radius: var(--radius);
+  padding: 8px 10px;
+  background: var(--panel-bg);
+  backdrop-filter: var(--panel-blur);
+  display: flex; align-items: center; justify-content: space-around;
+  min-height: 64px;
+}
+.hudMinimap { padding: 4px; }
+.hudColumn { text-align: center; min-width: 72px; }
+.hudLabel { font-size: 12px; color: var(--fg-dim); }
+.hudValue { font-size: 18px; }
+
+/* Overlays (menu/game over) */
+.overlayRoot {
+  position: fixed;
+  inset: 0;
+  display: grid;
+  place-items: center;
+  background: rgba(0,0,0,0.3);
+}
+.panel.modal {
+  border: 2px solid var(--line);
+  border-radius: 12px;
+  padding: 20px 24px;
+  width: min(520px, 90vw);
+  background: var(--panel-bg);
+  backdrop-filter: var(--panel-blur);
+  box-shadow: 0 10px 40px rgba(0,0,0,0.6);
+  text-align: center;
+}
+.title { margin: 4px 0 8px; letter-spacing: 2px; }
+.subtitle { margin: 0 0 14px; color: var(--fg-dim); }
+.row { display: flex; gap: 12px; justify-content: center; }
+.statsRow {
+  display: grid;
+  grid-template-columns: repeat(2,1fr);
+  gap: 8px 16px;
+  margin: 8px 0 16px;
+}
+.btn {
+  padding: 10px 14px;
+  border: 2px solid var(--line);
+  border-radius: 10px;
+  background: transparent;
+  color: var(--fg);
+  cursor: pointer;
+}
+.btn.primary {
+  border-color: var(--accent);
+  box-shadow: 0 0 12px rgba(156, 240, 109, 0.25) inset;
+}
+.btn:active { transform: translateY(1px); }
+.help { margin-top: 10px; color: var(--fg-dim); }
```
